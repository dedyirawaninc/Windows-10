Vagrant.configure("2") do |config|
  # Use Ubuntu 18.04 (Bionic Beaver) base box
  config.vm.box = "ubuntu/bionic64"

  # VM resources
  config.vm.provider "virtualbox" do |vb|
    vb.name = "Ubuntu 1804 LTS 64 Bionic Beaver A00"
    vb.memory = 16384
    vb.cpus = 8
    vb.customize [
        "createhd", 
        "--filename", 
        "Ubuntu 1804 LTS 64 Bionic Beaver A00.vdi", 
        "--size", 
        524288
    ]
    vb.customize [
        "storageattach", :id, 
        "--storagectl", 
        "SATA Controller", 
        "--port", 
        1, 
        "--device", 
        0, 
        "--type", 
        "hdd", 
        "--medium", 
        "Ubuntu 1804 LTS 64 Bionic Beaver A00.vdi"
    ]
  end

  # Networking
  config.vm.network "private_network", ip: "192.168.56.50"

  # Provisioning (optional)
  config.vm.provision "shell", inline: <<-SHELL
    sudo apt-get update -y
    sudo apt-get install -y curl git htop nmap
  SHELL
end